clc;
clear all;


load bouttons_in_areas.mat;       % all_moved_lines 
% d = 0;
% for i = 1 : size(all_moved_lines, 1)
%    this_line = all_moved_lines{i, 1};
%    d = d + get_distance(this_line);     
% end